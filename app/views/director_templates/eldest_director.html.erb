<nav>
  <a href="/">
    Home
  </a>
</nav>

<h1> Eldest director </h1>

<div>

The eldest director in our dataset right now is <a href = "/directors/<%= @older.id %> "><%= @older.name %></a>, born on <%= @older.dob.strftime("%B %e, %Y") %>.

</div>
